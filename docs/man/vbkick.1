man(1)                          vbkick man page                         man(1)



NNAAMMEE
       vbkick  -  the  automatic  Virtualbox  Guests and/or Vagrant base boxes
       builder.

SSYYNNOOPPSSIISS
       vvbbkkiicckk _A_C_T_I_O_N _V_M___N_A_M_E

DDEESSCCRRIIPPTTIIOONN
       vbkick is easy in use and free bash script (with enabled  POSIX  mode).
       It  automate/simplify  Virtualbox Guest and Vagrant base box build pro-
       cess (only 1-2 actions are needed). New Virtual machine is describe  as
       a code in _d_e_f_i_n_i_t_i_o_n_._c_f_g file.  Task is mostly about running VBoxManage
       command in proper order with proper options. Uses also ssh and scp com-
       mands to complete the job.

       Kickstart  and  postinstall  files  are serve to VM via local webserver
       started in background.

       vbkick is supported by _c_o_n_v_e_r_t___2___s_c_a_n_c_o_d_e_._p_y  tool,  which  help  enter
       key-strokes into a VirtualBox guest programmatically from the host.

AACCTTIIOONNSS
       bbuuiilldd  Builds a new Virtualbox Guest from given _d_e_f_i_n_i_t_i_o_n_._c_f_g. defini-
              tion.cfg file must exist in directory where command is run.

       ppoossttiinnssttaallll
              Run defined postinstall scripts via  SSH  on  Virtualbox  Guest.
              _d_e_f_i_n_i_t_i_o_n_._c_f_g  file  is  required in directory where command is
              run.

       vvaalliiddaattee
              Run defined features scripts via SSH on Virtualbox Guest.  Check
              whether new VM Guest has expected behaviour. _d_e_f_i_n_i_t_i_o_n_._c_f_g file
              is required in directory where command is run.

       eexxppoorrtt Exports Virtualbox Guest as a Vagrant base box (base image for a
              future  VMs)  -  _V_M___N_A_M_E_._b_o_x  gzipped  file  is created. _d_e_f_i_n_i_-
              _t_i_o_n_._c_f_g file is required in directory where command is run.

       uuppddaattee Updates Virtualbox  Guest  Additions  and  runs  defined  update
              scripts  via  SSH  on  Virtualbox  Guest. _d_e_f_i_n_i_t_i_o_n_._c_f_g file is
              required in directory where command is run.

       ddeessttrrooyy
              Remove virtual machine from Virtualbox.  Doing  this  will  also
              remove  the files containing the machine’s virtual hard disks if
              they are not in use by another machine.


       FFIILLEESS

       /usr/local/bin/vbkick and /usr/local/bin/convert_2_scancode.py

SSEEEE AALLSSOO
       vboxmanage(1) bash(1) ssh(1) scp(1)

AAUUTTHHOORR
       Kamil Wilas (wilas.pl)

CCOOPPYYRRIIGGHHTT
       vbkick is distributed under  the  MIT  License.  See  LICENSE  file  in
       https://github.com/wilas/vbkick for details.

       The site includes also usage examples and other docs which are not cov-
       ered by this manual page.



0.4.0                           10 August 2013                          man(1)
